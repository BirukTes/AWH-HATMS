<% if model.errors.any? %>
  <div id="error_explanation alert alert-danger errors">
    <h3><%= pluralize(model.errors.count, "error") %> prohibited this <%= model.class.downcase %> from being saved:</h3>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
