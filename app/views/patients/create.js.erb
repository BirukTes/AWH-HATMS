// Remove old validation messages
$(".alert").remove();

$(document).ready(function(){
    $(window).scrollTop(0);
});

// Display new validation messages
$('.error_holder').append('<%= escape_javascript(render('shared/errors', model: @patient))%>');

