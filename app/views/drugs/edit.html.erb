<h1>Editing Drug</h1>

<%= render 'form', drug: @drug %>

<% content_for(:navigationButtons) do %>
  <%= link_to('View', @drug, class: 'btn btn-secondary') %> &nbsp;
  <%= link_to('Back', drugs_path, class: 'btn btn-secondary') %>
<% end %>