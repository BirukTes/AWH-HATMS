<%= form_with(model: drug, local: true) do |form| %>
  <% if drug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(drug.errors.count, "error") %> prohibited this drug from being saved:</h2>

      <ul>
        <% drug.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group col-3">
    <%= form.label :code %>
    <%= form.text_field :code, id: :drug_code, class: 'form-control' %>
  </div>

  <div class="form-group col-3">
    <%= form.label :name %>
    <%= form.text_field :name, id: :drug_name, class: 'form-control' %>
  </div>

  <div class="col">
    <%= form.submit(class: 'btn btn-primary') %>
  </div>
<% end %>
