<% patient = Patient.find(@treatment.patient_id) %>
<h1>Treatment Update for <%= patient.person.firstName + ' ' + patient.person.lastName %></h1>
<%= render 'form', treatment: @treatment, patient: patient%>
