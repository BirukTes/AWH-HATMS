<% patient = Patient.find(@admission.patient_id) %>
<h1>Admission Update For <%= patient.person.firstName + ' ' + patient.person.lastName %></h1>
<%= render('form', admission: @admission, patient: patient) %>
