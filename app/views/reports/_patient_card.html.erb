<table class="table table-bordered table-striped">
  <thead class="thead-light">
  <tr>
    <th>Patient No</th>
    <th>Patient Name</th>
    <th>Ward No</th>
    <th>Ward Name</th>
  </tr>
  </thead>
  <tbody>
  <% patient_card_admission = admissions.where(admissionDate: Date.today.beginning_of_week).first %>
  <% if patient_card_admission.blank? %>
    <tr>
      <td colspan="5"><p>No patient found from this date.</p></td>
    </tr>
  <% else %>
    <% patient = patient_card_admission.patient %>
    <% ward = patient_card_admission.ward %>
    <% prescriptions = patient&.prescriptions %>
    <tr>
      <td><%= patient.id %></td>
      <td><%= patient.person.firstName + ' ' + patient.person.lastName %></td>
      <td><%= ward.wardNumber %></td>
      <td><%= ward.name %></td>
    </tr>

    <thead class="thead-light">
    <tr>
      <th>Date</th>
      <th>Drug Code</th>
      <th>Drug Name</th>
      <th>Dosage</th>
      <th>Length of Treatment</th>
    </tr>
    </thead>
    <tr>
    <% if prescriptions.empty? %>
      <td colspan="3"><p>No prescriptions found.</p></td>
    <% else %>
      <% prescriptions&.each do |prescription| %>
        <% prescription&.drugs.each do |drug| %>
          <td><%= drug.code %> </td>
          <td><%= drug.name %> </td>
        <% end %>
        <td><%= prescription.dosage %></td>
        <td><%= prescription.treatmentLength %></td>
      <% end %>
    <% end %>
    </tr>
  <% end %>
  </tbody>
  </table>